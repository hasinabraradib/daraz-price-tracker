# Copy to .env.local (gitignored by Next.js's default .gitignore).
# Base URL of the FastAPI backend — see api/app/main.py's CORS config
# for the other half of this pairing (FRONTEND_ORIGIN in the repo's
# root .env must match wherever this frontend actually runs).
NEXT_PUBLIC_API_URL=http://localhost:8000
